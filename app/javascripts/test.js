window.addEventListener('load',()=>{
	let principal = document.querySelector('main');
	let texto = document.createElement('p');
	texto.textContent = "sistema de javascript completado";
	principal.appendChild(texto);
});